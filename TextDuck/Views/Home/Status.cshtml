@model IQueryable<TextDuck.Models.srtFiles>

<h1> srt Textar i Vinnslu</h1>

@foreach (var item in Model)
{
      <div class="jumbotron">
        <p class="Date">
            @Html.DisplayFor(modelItem => item.Date)
            <span class="glyphicon glyphicon-time"></span>
        </p>
        <h2 class="Status">@Html.ActionLink(item.Title, "Status")</h2>
        @*@Html.DisplayFor(modelItem => item.Status) &nbsp;|&nbsp;*@
        @Html.DisplayFor(modelItem => item.Language) &nbsp;|&nbsp;
        @Html.DisplayFor(modelItem => item.Genre)&nbsp;|&nbsp;
        @Html.DisplayFor(modelItem => item.Category)
          <div>
              @Html.ActionLink("Breyta skrá", "TextBoxSrt", new { Id = item.Id }, null)&nbsp;|&nbsp;
              @Html.ActionLink("Breyta upplýsingum", "FileAppearanceChanges", new { Id = item.Id }, null)&nbsp;|&nbsp;
              @Html.ActionLink("Commenta", "AddComment", new { Id = item.Id }, null)
          </div>
    </div>

}

<div>
    @Html.ActionLink("Aftur a forsidu", "Index")
</div>
